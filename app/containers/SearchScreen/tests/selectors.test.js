// import { selectSearchScreenDomain } from '../selectors';

describe('selectSearchScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
